const startCommand = {
  command: 'start',
  description: 'Menu',
};

const pointsCommand = {
  command: 'points',
  description: 'Check your points',
};

export const commands = [startCommand, pointsCommand];
